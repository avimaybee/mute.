<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotificationMenace" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mute_obsidian</item>
        <item name="colorOnPrimary">@color/mute_platinum</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mute_orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorBackground">@color/mute_obsidian</item>
        <item name="colorSurface">@color/mute_charcoal</item>
        <item name="colorOnSurface">@color/mute_platinum</item>
    </style>
</resources>